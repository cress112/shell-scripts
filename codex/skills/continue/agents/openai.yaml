interface:
  display_name: "Continue"
  short_description: "Resume work from SQLite memory"
  default_prompt: "Use $continue to resolve the target project path, query ~/.codex/sqlite/user-memory.sqlite through ~/.codex/scripts/codex-memory context/search, summarize the current state, and ask what to do next. Do not read Markdown memory files."
